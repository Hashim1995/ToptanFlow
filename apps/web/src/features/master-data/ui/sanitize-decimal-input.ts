/**
 * @deprecated Import from `../../../shared/decimal` instead.
 * Kept as a thin re-export so existing feature imports keep working.
 */
export {
  normalizeDecimalInput,
  sanitizeDecimalInput,
} from '../../../shared/decimal';
